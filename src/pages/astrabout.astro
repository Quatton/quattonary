---
import { getEntry } from "astro:content";
import MarkdownLayout from "src/layouts/MarkdownLayout.astro";
const astrabout = await getEntry("front", "astrabout");
const { Content, headings } = await astrabout.render();
---

<MarkdownLayout title="Astrabout">
  <Content />
</MarkdownLayout>
