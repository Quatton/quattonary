---
import { getEntry } from "astro:content";
import MarkdownLayout from "src/layouts/MarkdownLayout.astro";
const notYet = await getEntry("front", "astrabout");
const { Content, headings } = await notYet.render();
---

<MarkdownLayout title="Too small!">
  <Content />
</MarkdownLayout>
